node_modules/

# Local agent/project insights dashboard — regenerated by `node docs/dashboard.mjs`, never hosted.
# Gitignored on purpose: its content (finding/verdict summaries) shouldn't accumulate in git history
# as a side effect of viewing it locally.
docs/dashboard.html
